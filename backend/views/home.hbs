{{!-- <!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Afnan — Coming Soon</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      /* small extra for a smooth animated gradient background */
      @keyframes floatBG {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }
      .gradient-bg {
        background: linear-gradient(120deg,#0ea5e9 0%, #7c3aed 50%, #ef4444 100%);
        background-size: 200% 200%;
        animation: floatBG 8s ease infinite;
      }
    </style>
  </head>
  <body class="min-h-screen flex items-center justify-center gradient-bg text-white">
    <main class="w-full max-w-3xl p-8">
      <div class="backdrop-blur-sm bg-white/10 rounded-2xl shadow-2xl p-10 text-center">
        <h1 class="text-4xl md:text-6xl font-extrabold tracking-tight mb-4">My name is <span class="text-yellow-300">Afnan</span></h1>
        <p class="text-xl md:text-2xl opacity-90 mb-6">My project is <span class="font-semibold">coming soon</span>.</p>

        <div class="mt-6 flex flex-col sm:flex-row items-center justify-center gap-4">
          <a href="#" class="px-6 py-3 rounded-full bg-yellow-300 text-black font-semibold hover:scale-105 transform transition">Notify me</a>
          <a href="#" class="px-6 py-3 rounded-full border border-white/30 hover:bg-white/10 transition">Learn more</a>
        </div>

        <p class="mt-6 text-sm opacity-80">Want a countdown, mailing-list form, or custom logo? Tell me and I'll add it — no fluff.</p>
      </div>

      <footer class="mt-6 text-center text-white/70 text-sm">&copy; <span id="year"></span> Afnan. All rights reserved.</footer>
    </main>

    <script>
      document.getElementById('year').textContent = new Date().getFullYear();
    </script>
  </body>
</html> --}}
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Afnan — Coming Soon</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      @keyframes floatBG { 0%{background-position:0% 50%}50%{background-position:100% 50%}100%{background-position:0% 50%} }
      .gradient-bg{ background: linear-gradient(120deg,#0ea5e9 0%, #7c3aed 40%, #06b6d4 70%); background-size:200% 200%; animation:floatBG 12s ease infinite; }
      .blob { filter: blur(40px); opacity: .35; }
      .glass { background: linear-gradient(180deg, rgba(255,255,255,0.06), rgba(255,255,255,0.02)); backdrop-filter: blur(8px); }
      /* subtle focus ring for keyboard users */
      .focus-ring:focus { outline: 3px solid rgba(99,102,241,0.18); outline-offset: 2px; }
    </style>
  </head>
  <body class="min-h-screen gradient-bg flex items-center justify-center text-slate-50">
    <!-- decorative blobs -->
    <div aria-hidden="true" class="fixed inset-0 -z-10 overflow-hidden">
      <svg class="absolute -left-10 top-10 w-96 h-96 blob" viewBox="0 0 600 600" xmlns="http://www.w3.org/2000/svg">
        <g transform="translate(300,300)">
          <path d="M120,-150C160,-120,200,-80,210,-30C220,20,200,70,160,110C120,150,60,175,10,160C-40,145,-80,90,-120,50C-160,10,-200,-20,-190,-70C-180,-120,-120,-180,-60,-190C0,-200,80,-180,120,-150Z" fill="#8b5cf6" />
        </g>
      </svg>
      <svg class="absolute right-0 bottom-0 w-80 h-80 blob" viewBox="0 0 600 600" xmlns="http://www.w3.org/2000/svg">
        <g transform="translate(300,300)">
          <path d="M120,-150C160,-120,200,-80,210,-30C220,20,200,70,160,110C120,150,60,175,10,160C-40,145,-80,90,-120,50C-160,10,-200,-20,-190,-70C-180,-120,-120,-180,-60,-190C0,-200,80,-180,120,-150Z" fill="#06b6d4" />
        </g>
      </svg>
    </div>

    <main class="w-full max-w-5xl p-6 lg:p-12">
      <div class="glass rounded-3xl p-8 lg:p-12 shadow-2xl shadow-black/40 border border-white/6">
        <div class="flex items-center justify-between gap-6">
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 rounded-full bg-white/10 flex items-center justify-center ring-1 ring-white/6">
              <span class="text-lg font-bold text-yellow-300">A</span>
            </div>
            <div>
              <p class="text-sm uppercase tracking-wider text-white/70">Hi, I'm</p>
              <h1 class="text-3xl sm:text-4xl lg:text-5xl font-extrabold leading-tight">Afnan<span class="text-transparent bg-clip-text bg-gradient-to-r from-yellow-300 via-amber-400 to-rose-400">.</span></h1>
            </div>
          </div>

          <nav aria-label="social" class="flex items-center gap-3">
            <a class="p-2 rounded-md hover:bg-white/6 focus-ring" href="#" aria-label="Twitter">🐦</a>
            <a class="p-2 rounded-md hover:bg-white/6 focus-ring" href="#" aria-label="GitHub">💻</a>
            <a class="p-2 rounded-md hover:bg-white/6 focus-ring" href="#" aria-label="LinkedIn">🔗</a>
          </nav>
        </div>

        <section class="mt-8 grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
          <div>
            <p class="text-lg text-white/85">My project is <span class="font-semibold">coming soon</span>. We're building something modern — clean UI, fast performance, and a product people actually want.</p>

            <div class="mt-6 flex flex-wrap gap-3">
              <a href="#subscribe" class="inline-flex items-center gap-2 px-5 py-3 rounded-full bg-gradient-to-r from-yellow-300 to-amber-400 text-black font-semibold shadow-lg hover:scale-105 transition-transform focus-ring">Notify me</a>
              <a href="#features" class="inline-flex items-center gap-2 px-5 py-3 rounded-full border border-white/10 hover:bg-white/4 transition focus-ring">See features</a>
            </div>

            <div class="mt-6">
              <div class="inline-flex items-center gap-3 bg-white/6 rounded-full px-4 py-2">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" aria-hidden><path d="M12 2v6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>
                <div>
                  <div class="text-sm text-white/80">Launch in</div>
                  <div id="countdown" class="font-mono text-xl sm:text-2xl font-semibold">00d 00h 00m 00s</div>
                </div>
              </div>
            </div>

            <p class="mt-6 text-sm text-white/70">No fluff. If you want invitations, early access, or collaboration — subscribe and I'll personally reach out to serious people only.</p>
          </div>

          <aside class="bg-gradient-to-br from-white/6 to-white/3 border border-white/6 rounded-2xl p-6 shadow-inner">
            <form id="subscribe" class="grid gap-4" onsubmit="(e=>{e.preventDefault(); alert('Thanks — subscription demo only. Integrate your backend or Mailchimp.')})(event)">
              <label for="email" class="sr-only">Email address</label>
              <input id="email" type="email" required placeholder="Enter your email" class="w-full px-4 py-3 rounded-lg bg-white/5 placeholder:text-white/60 focus:ring-2 focus:ring-amber-300 focus:outline-none" />
              <div class="flex gap-3">
                <button class="flex-1 px-4 py-3 rounded-lg bg-gradient-to-r from-yellow-300 to-amber-400 font-semibold hover:scale-102 transition-transform focus-ring" type="submit">Get early access</button>
                <button type="button" class="px-4 py-3 rounded-lg border border-white/10 hover:bg-white/4 focus-ring">I'll wait</button>
              </div>

              <div class="text-xs text-white/60">We hate spam. Unsubscribe anytime. This demo form is client-side only.</div>
            </form>

            <div class="mt-6 grid grid-cols-3 gap-3 text-center text-sm text-white/70">
              <div>
                <div class="font-bold text-lg">—</div>
                <div class="mt-1">Fast</div>
              </div>
              <div>
                <div class="font-bold text-lg">—</div>
                <div class="mt-1">Modern</div>
              </div>
              <div>
                <div class="font-bold text-lg">—</div>
                <div class="mt-1">Accessible</div>
              </div>
            </div>
          </aside>
        </section>

        <hr class="my-8 border-white/6" />

        <div id="features" class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div class="p-4 rounded-xl bg-white/3">
            <h3 class="font-semibold">Beautiful UI</h3>
            <p class="text-sm text-white/70 mt-2">Pixel-perfect components with Tailwind utilities so you can ship faster.</p>
          </div>
          <div class="p-4 rounded-xl bg-white/3">
            <h3 class="font-semibold">Performance</h3>
            <p class="text-sm text-white/70 mt-2">Optimized for real users — small bundles, lazy loading, and semantic HTML.</p>
          </div>
          <div class="p-4 rounded-xl bg-white/3">
            <h3 class="font-semibold">Privacy-first</h3>
            <p class="text-sm text-white/70 mt-2">Your data stays yours. Integrate the provider you trust (or none at all).</p>
          </div>
        </div>

        <footer class="mt-8 flex items-center justify-between text-sm text-white/60">
          <div>&copy; <span id="year"></span> Afnan</div>
          <div>Built with <span title="TailwindCSS">Tailwind</span></div>
        </footer>
      </div>
    </main>

    <script>
      // nice countdown — change the date below to your launch date (UTC)
      (function(){
        const launch = new Date(Date.UTC(2026,0,15,0,0,0)); // Jan 15, 2026 UTC
        const el = document.getElementById('countdown');
        function tick(){
          const now = new Date();
          let diff = Math.max(0, launch - now);
          const d = Math.floor(diff / (1000*60*60*24));
          diff -= d*(1000*60*60*24);
          const h = Math.floor(diff/(1000*60*60)); diff -= h*(1000*60*60);
          const m = Math.floor(diff/(1000*60)); diff -= m*(1000*60);
          const s = Math.floor(diff/1000);
          el.textContent = `${String(d).padStart(2,'0')}d ${String(h).padStart(2,'0')}h ${String(m).padStart(2,'0')}m ${String(s).padStart(2,'0')}s`;
        }
        tick(); setInterval(tick,1000);
      })();

      // set year
      document.getElementById('year').textContent = new Date().getFullYear();
    </script>

  </body>
</html>